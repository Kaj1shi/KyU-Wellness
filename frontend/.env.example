# Vite frontend environment
# Copy to frontend/.env.local for local overrides if needed.

# Local (Vite proxy can also use relative /api)
VITE_API_URL=http://localhost:8000/api

# Production example (Render / Railway API):
# VITE_API_URL=https://kyu-wellness-api.onrender.com/api
